name: CI

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

env:
  DATABASE_URL: postgres://postgres:postgres@localhost:5432/test

jobs:
  build:
    runs-on: ubuntu-latest  # Changed from 24.04 (not supported) to latest

    strategy:
      matrix:
        include:
          # Test multiple Prisma versions for Node <=18
          - node-version: 18.18.0
            prisma-version: 4.9.0
          - node-version: 18.18.0
            prisma-version: 4.16.0

          # Test multiple Prisma versions for Node >=18
          - node-version: 18.18.0
            prisma-version: latest
          - node-version: lts/*
            prisma-version: latest

    services:
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "yarn"

      - run: yarn add prisma@${{ matrix.prisma-version }} @prisma/client@${{ matrix.prisma-version }}
      - run: yarn install
      - run: yarn build
      - run: yarn prisma generate
      - run: yarn prisma db push
      - run: yarn test
